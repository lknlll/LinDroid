[TOC]

### 事件源头

在 [Activity绘制流程](https://github.com/lknlll/LinDroid/blob/LinDroid/AndroidAssemble/Android%E5%8E%9F%E7%90%86/View%E6%B8%B2%E6%9F%93_%E6%9C%BA%E5%88%B6_%E8%87%AA%E5%AE%9A%E4%B9%89/Activity%E7%BB%98%E5%88%B6%E6%B5%81%E7%A8%8B-WindowViewRootImpl.md) 中分析了ViewRootImpl如何实例化的，并且在setView中通过requestLayout发起绘制；

这里从事件传递的角度再看

```

    public void setView(View view, WindowManager.LayoutParams attrs, 
        View panelParentView, int userId) {
        
        synchronized (this) {
            if (mView == null) {
                ...
                InputChannel inputChannel = null;
                if ((mWindowAttributes.inputFeatures
                        & WindowManager.LayoutParams.INPUT_FEATURE_NO_INPUT_CHANNEL) == 0) {
                    inputChannel = new InputChannel();
                }
                ...
                try {
                    ...
                    res = mWindowSession.addToDisplayAsUser(mWindow, mWindowAttributes,
                            getHostVisibility(), mDisplay.getDisplayId(), userId,
                            mInsetsController.getRequestedVisibility(), inputChannel, mTempInsets,
                            mTempControls);
                    ...
                } catch (RemoteException e) {
                    ...
                } finally {
                    ...
                }
                ...
                if (inputChannel != null) {
                    ...
                    mInputEventReceiver = new WindowInputEventReceiver(inputChannel,
                            Looper.myLooper());

                    ...
                }
            }
        }
    }

```

创建了一个WindowInputEventReceiver用来接收事件

### WindowInputEventReceiver分发到DecorView

创建WindowInputEventReceiver时，可以看到传入了一个InputChannel

InputChannel就是底层通知上层事件的核心，系统服务捕获到屏幕事件后，会通过它通知到上层，也就是WindowInputEventReceiver

```
    final class WindowInputEventReceiver extends InputEventReceiver {
        ...

        @Override
        public void onInputEvent(InputEvent event) {
            ...
            if (processedEvents != null) {
                if (processedEvents.isEmpty()) {
                    // InputEvent consumed by mInputCompatProcessor
                    finishInputEvent(event, true);
                } else {
                    for (int i = 0; i < processedEvents.size(); i++) {
                        enqueueInputEvent(
                                processedEvents.get(i), this,
                                QueuedInputEvent.FLAG_MODIFIED_FOR_COMPATIBILITY, true);
                    }
                }
            } else {
                enqueueInputEvent(event, this, 0, true);
            }
        }

        ...
    }

```

onInputEvent -> enqueueInputEvent -> 判断立即执行还是延迟处理 -> deliverInputEvent

    private void deliverInputEvent(QueuedInputEvent q) {
        ...
        try {
            ...

            InputStage stage;
            if (q.shouldSendToSynthesizer()) {
                stage = mSyntheticInputStage;
            } else {
                stage = q.shouldSkipIme() ? mFirstPostImeInputStage : mFirstInputStage;
            }

            ...

            if (stage != null) {
                handleWindowFocusChanged();
                stage.deliver(q);
            } else {
                finishInputEvent(q);
            }
        } finally {
            Trace.traceEnd(Trace.TRACE_TAG_VIEW);
        }
    }
    
    
通过stage对QueuedInputEvent q进行了deliver

这里回到setView中寻找stage

                // Set up the input pipeline.
                CharSequence counterSuffix = attrs.getTitle();
                mSyntheticInputStage = new SyntheticInputStage();
                
                InputStage viewPostImeStage = new ViewPostImeInputStage(mSyntheticInputStage);
                InputStage nativePostImeStage = new NativePostImeInputStage(viewPostImeStage,
                        "aq:native-post-ime:" + counterSuffix);
                InputStage earlyPostImeStage = new EarlyPostImeInputStage(nativePostImeStage);
                InputStage imeStage = new ImeInputStage(earlyPostImeStage,
                        "aq:ime:" + counterSuffix);
                InputStage viewPreImeStage = new ViewPreImeInputStage(imeStage);
                InputStage nativePreImeStage = new NativePreImeInputStage(viewPreImeStage,
                        "aq:native-pre-ime:" + counterSuffix);

                mFirstInputStage = nativePreImeStage;
                mFirstPostImeInputStage = earlyPostImeStage;
                mPendingInputEventQueueLengthCounterName = "aq:pending:" + counterSuffix;

重点来看第一个 InputStage ViewPostImeInputStage

    final class ViewPostImeInputStage extends InputStage {
        ...

        @Override
        protected int onProcess(QueuedInputEvent q) {
            if (q.mEvent instanceof KeyEvent) {
                return processKeyEvent(q);
            } else {
                final int source = q.mEvent.getSource();
                if ((source & InputDevice.SOURCE_CLASS_POINTER) != 0) {
                    return processPointerEvent(q);
                } else if ((source & InputDevice.SOURCE_CLASS_TRACKBALL) != 0) {
                    return processTrackballEvent(q);
                } else {
                    return processGenericMotionEvent(q);
                }
            }
        }

        ...

        private int processPointerEvent(QueuedInputEvent q) {
            final MotionEvent event = (MotionEvent)q.mEvent;

            mAttachInfo.mUnbufferedDispatchRequested = false;
            mAttachInfo.mHandlingPointerEvent = true;
            boolean handled = mView.dispatchPointerEvent(event);
            ...
            return handled ? FINISH_HANDLED : FORWARD;
        }

        ...
    }
    
InputStage的deliver过程先略过，最终会通过onProcess来区分事件处理；

事件交给了processPointerEvent来处理。在这里可以看到执行了mView.dispatchPointerEvent(event)，这个mView就是ViewRootImpl.setView时传入的DecorView；

这样事件就分发到了DecorView

### DecorView和Activity之间的分发


    //View
    public final boolean dispatchPointerEvent(MotionEvent event) {
        if (event.isTouchEvent()) {
            return dispatchTouchEvent(event);
        } else {
            return dispatchGenericMotionEvent(event);
        }
    }
    
这里由于View是DecorView，dispatchTouchEvent会进入DecorView重写的逻辑
    
```    
    //DecorView
    @Override
    public boolean dispatchTouchEvent(MotionEvent ev) {
        final Window.Callback cb = mWindow.getCallback();
        return cb != null && !mWindow.isDestroyed() && mFeatureId < 0
                ? cb.dispatchTouchEvent(ev) : super.dispatchTouchEvent(ev);
    }
```

它通过Window获取了callback，然后执行了callback的dispatchTouchEvent

在[Activity绘制流程](https://github.com/lknlll/LinDroid/blob/LinDroid/AndroidAssemble/Android%E5%8E%9F%E7%90%86/View%E6%B8%B2%E6%9F%93_%E6%9C%BA%E5%88%B6_%E8%87%AA%E5%AE%9A%E4%B9%89/Activity%E7%BB%98%E5%88%B6%E6%B5%81%E7%A8%8B-WindowViewRootImpl.md) 中分析Window的创建的时候提到过，Activity本身实现了Window.Callback接口，并设置给了window的callback。所以这里的callback其实就是Activity，这样事件就传递到了Activity


    public boolean dispatchTouchEvent(MotionEvent ev) {
        if (ev.getAction() == MotionEvent.ACTION_DOWN) {
            onUserInteraction();
        }
        if (getWindow().superDispatchTouchEvent(ev)) {
            return true;
        }
        return onTouchEvent(ev);
    }

Activity调用了Window的superDispatchTouchEvent函数，它的实现在PhoneWindow；

    @Override
    public boolean superDispatchTouchEvent(MotionEvent event) {
        return mDecor.superDispatchTouchEvent(event);
    }
    
这样又传递回DecorView了

    public boolean superDispatchTouchEvent(MotionEvent event) {
        return super.dispatchTouchEvent(event);
    }
    
在DecorView中执行了`super.dispatchTouchEvent(event);`，它的父类就是ViewGroup，这样就进入了ViewGroup分发事件的环节了

### ViewGroup和View的事件传递

#### 基本三方法 

##### ViewGroup最简逻辑

```
    public boolean dispatchTouchEvent(MotionEvent ev) {
        boolean consume = false;
        
        // Note
        // 只有ViewGroup有onInterceptTouchEvent，默认返回false
        
        
        if (onInterceptTouchEvent(ev)) {
            if (!(mOnTouchListener != null && mOnTouchListener.onTouchEvent(ev))) {
                consume = onTouchEvent(ev);
            }
        } else {
            consume = child.dispatchTouchEvent(ev);
        }
        return consume;
    }
    
    public void onTouchEvent(MotionEvent ev) {
        boolean consume = false;
        if (mOnClickListener != null) {
            mOnClickListener.onClick(this);
            consume = true;
        }
    return consume;

```

##### View 最简逻辑


```
    public boolean dispatchTouchEvent(MotionEvent ev) {
        boolean consume = false;
        consume = onTouchEvent(ev);
        return consume;
    }
 
    public void onTouchEvent(MotionEvent ev) {
        boolean consume = false;
        if (mOnClickListener != null) {
            mOnClickListener.onClick(this);
            consume = true;
        }
    return consume;
    
```

假设Activity布局为一个LinearLayout内部一个Button，不做额外处理

点击Button后事件传递流程为

DecorView.dispatchTouchEvent()进入，onInterceptTouchEvent默认为false，

进入else继续分发，寻找DecorView的child，LinearLayout

进入LinearLayout.dispatchTouchEvent(); 

LinearLayout.dispatchTouchEvent()判断LinearLayout.onInterceptTouchEvent

依旧进入else继续分发，寻找LinearLayout的child，Button

进入Button.dispatchTouchEvent();

Button.dispatchTouchEvent()进入Button.onTouchEvent()进行消费

Button.onClick()消费掉后其onTouchEvent()返回true

从而Button.dispatchTouchEvent()返回true

Button的parent，LinearLayout.dispatchTouchEvent()返回true

LinearLayout的parent，DecorView.dispatchTouchEvent()返回true

//Todo
MotionEvent详解
https://www.gcssloop.com/customview/motionevent.html


//Todo 各类相应方法优先级 onClick onLongClick 等
 onTouch和onTouchEvent有什么区别，又该如何使用？

从源码中可以看出，这两个方法都是在View的dispatchTouchEvent中调用的，onTouch优先于onTouchEvent执行。如果在onTouch方法中通过返回true将事件消费掉，onTouchEvent将不会再执行。

 

另外需要注意的是，onTouch能够得到执行需要两个前提条件，第一mOnTouchListener的值不能为空，第二当前点击的控件必须是enable的。因此如果你有一个控件是非enable的，那么给它注册onTouch事件将永远得不到执行。对于这一类控件，如果我们想要监听它的touch事件，就必须通过在该控件中重写onTouchEvent方法来实现。

带源码详解
https://www.gcssloop.com/customview/dispatch-touchevent-source.html

//Todo 实际场景分析结合MotionEvent

在onInterceptTouchEvent中返回true时，MOVE与UP事件如何执行？
onInterceptTouchEvent在使用的时候必须要知道两点内容：


onInterceptTouchEvent一旦返回true，也就是被拦截之后后续的MOVE、UP方法就不会在执行这个方法了，而是直接去执行自身的onTouch方法。onInterceptTouchEvent一旦被拦截，一次事件只执行一次，后续MOVE与UP不再执行。


假设此时有一个ViewGroupA，还有一个子ViewB；而当ViewGroupA在进行拦截的时候，并没有去拦截Down事件，而是去拦截了ViewGroupA的Move事件，此时Down事件会直接去ViewB，而第一个MOVE事件不会传给自身的onTouchEvent事件，而是以一个Cancel传递给ViewB，而后面的MOVE才传递给自身的onTouch事件。



//Todo
Android事件分发机制详解与实战
https://blog.csdn.net/geduo_83/article/details/86560896


onInterceptTouchEvent和onTouchEvent调用关系详解
https://www.cnblogs.com/awkflf11/p/12635429.html

核心分发逻辑源码解析
https://juejin.cn/post/6920883974952714247

事件到底是先到DecorView还是先到Window的
https://juejin.cn/post/7267482030329675836

滑动事件消费监控，快速定位 View 事件消费
https://juejin.cn/post/7280735834125582393

面试题
https://mp.weixin.qq.com/s/_18HWsMbtCSw36ezwSuzYg

输入事件传递流程和InputManagerService的诞生
https://liuwangshu.cn/framework/ims/1-ims-produce.html

Android事件分发树分析 
https://mp.weixin.qq.com/s/s7r_BAlsCHeMqQWxtXt_lg

requestDisallowInterceptTouchEvent
https://mp.weixin.qq.com/s/3CVmuV7SaNjMo0TmUtERKQ

View 事件分发，就像外地人上了黑车-递归
https://juejin.cn/post/6844903894103883789

Android Touch事件分发超详细解析（附源码）
https://www.jianshu.com/p/bc4c9e5f4b1c